@model Elmah.Web.Models.HomeViewData

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutPage.cshtml";
}
<div class="row">
    <div class="col-sm-12">
        <h1>Application Health</h1>
    </div>
</div>
<div class="bs-docs-grid">
    <div class="row">
        
    </div>
</div>
<div id="#empty"></div>
<!--Reference the autogenerated SignalR hub script. -->
<script src="~/signalr/hubs"></script>
<script type="text/javascript">
    $(function () {
        // Reference the auto-generated proxy for the hub.
        var applicationStateHub = $.connection.applicationStateHub;

        // Create a function that the hub can call back to display states.
        applicationStateHub.client.get = function (name, message) {
            // Add the message to the page.
            /*$('#empty').append('<li><strong>' + htmlEncode(name)
                + '</strong>: ' + htmlEncode(message) + '</li>');*/
        };

        function init() {
            applicationStateHub.client.get().done(function (states) {
                
                $.each(states, function () {
                    
                });
            });
        }

        $.connection.hub.start().done(init);
    });
</script>